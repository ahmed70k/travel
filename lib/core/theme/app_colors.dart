import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const Color backgroundStart = Color(0xFF0f0c29);
  static const Color backgroundMiddle = Color(0xFF1a1638);
  static const Color backgroundEnd = Color(0xFF0a081c);

  // Primary Gradients & Accents
  static const Color primary = Color(0xFF8b5cf6);
  static const Color secondary = Color(0xFFec4899);

  // States
  static const Color success = Color(0xFF22c55e);
  static const Color warning = Color(0xFFeab308);
  static const Color info = Color(0xFF3b82f6);
  static const Color error = Color(0xFFef4444);

  // Glass & Borders
  static const Color glassBackground = Color(0x990f0c29); // 60% opacity
  static const Color glassBorder = Color(0x338b5cf6); // 20% opacity primary

  static const Color cardGlow = Color(0x668b5cf6); // 40% primary

  // Text Colors
  static const Color textMain = Colors.white;
  static const Color textMuted = Color(0xFF9ca3af); // gray-400
}
